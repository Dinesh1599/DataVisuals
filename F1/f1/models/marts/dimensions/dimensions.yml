version: 2

models:

  - name: dim_drivers
    columns:
      - name: driver_sk
        tests: [unique, not_null]
      - name: driver_id
        tests: [unique, not_null]

  - name: dim_constructors
    columns:
      - name: constructor_sk
        tests: [unique, not_null]
      - name: constructor_id
        tests: [unique, not_null]

  - name: dim_circuits
    columns:
      - name: circuit_sk
        tests: [unique, not_null]
      - name: circuit_id
        tests: [unique, not_null]

  - name: dim_races
    columns:
      - name: race_sk
        tests: [unique, not_null]
      - name: race_id
        tests: [unique, not_null]

  - name: dim_status
    columns:
      - name: status_sk
        tests: [unique, not_null]
      - name: status_id
        tests: [unique, not_null]

  - name: dim_seasons
    columns:
      - name: season_sk
        tests: [unique, not_null]
      - name: season_year
        tests: [unique, not_null]
